apiVersion: v1
kind: Service
metadata:
  name: owokx-dashboard
  labels:
    app: owokx-dashboard
spec:
  type: ClusterIP
  selector:
    app: owokx-dashboard
    track: blue
  ports:
    - name: http
      port: 80
      targetPort: http
